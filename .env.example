# Exemplo de variáveis de ambiente (NUNCA commite o seu .env real)

# OpenAI
OPENAI_API_KEY=

# PostgreSQL
DB_HOST=localhost
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=
DB_PORT=5432

# Ingest / processamento
DOCUMENT_URL=              # URL pública para .docx (opcional)
ARQUIVO_LOCAL=documento_temp.docx

# Agent / fallback
SECONDARY_AGENT_PATH=salvar_pendencia_sanitizado.py
OLLAMA_MODEL=mistral

# App UI
CACHE_FILE=cache_respostas.json
